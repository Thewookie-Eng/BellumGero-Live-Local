-- Manage Maintenance Command
-- Displays character-owned structures and permits owner-only remote maintenance deposits.

ManageMaintenanceCommand = {
	name = "managemaintenance",
}

AddCommand(ManageMaintenanceCommand)

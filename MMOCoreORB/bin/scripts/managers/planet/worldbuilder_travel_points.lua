-- AUTO-GENERATED by Bellum Gero World Builder. Do not hand-edit.
worldBuilderTravelPoints = {
}

for planetName, points in pairs(worldBuilderTravelPoints) do
	local planet = _G[planetName]
	if planet ~= nil then
		planet.planetTravelPoints = planet.planetTravelPoints or {}
		for _, point in ipairs(points) do table.insert(planet.planetTravelPoints, point) end
	end
end
